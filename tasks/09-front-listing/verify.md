# Verify — 09 front-listing

## Сборка
- [ ] `cd front && npm run build` — зелёный (tsc strict, без `any`).
- [ ] Новых зависимостей в `front/package.json` не добавлено.

## Данные из API (визуально, dev на 5005 + запущенный back)
- [ ] `/projects` показывает работы из API (не хардкод). `/projects/:cat/:sub` — фильтр по подкатегории.
- [ ] Навигация по категориям/подкатегориям построена из API.
- [ ] Тайлы показываются целиком, `aspect-ratio` зарезервирован из `w/h` — **нет скачков layout**
      при загрузке картинок.
- [ ] Во время загрузки виден skeleton-тон (`--c-skeleton`); пустой результат и ошибка сети
      обрабатываются (не белый экран).

## Оба дерева
- [ ] Десктоп (`ProjectsScreen.tsx`) и мобайл (`MobileProjects.tsx`) оба берут данные из API
      (проверить ресайзом/эмуляцией <768px).

## Регрессии
- [ ] Masonry-раскладка/зазоры/число колонок не сломаны; занавес-герой работает как прежде.

## Done
Листинг управляется данными API в обоих деревьях; модалка/карусель — задача 10.
</content>
