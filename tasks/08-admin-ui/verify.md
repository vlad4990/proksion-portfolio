# Verify — 08 admin-ui

## Тесты (TDD)
- [ ] `cd admin && npm run test` (Vitest) — зелёный. Покрыты: валидация/маппинг форм,
      state machine загрузки картинок, вычисление payload для reorder.

## Сборка
- [ ] `cd admin && npm run build` — зелёный (tsc strict). Бандл `/front` не затронут.

## Сквозной путь (против запущенных `back` + `minio`)
- [ ] Создать категорию → подкатегорию → работу через UI; слаги сгенерированы.
- [ ] Загрузить несколько картинок в работу (виден прогресс); первая стала cover; можно сменить cover.
- [ ] Задать описание; отредактировать alt; удалить картинку (исчезает и из MinIO).
- [ ] Drag-n-drop меняет порядок (категорий/работ/картинок) — порядок сохраняется.
- [ ] Удаление категории/работы каскадит; подтверждение спрашивается.
- [ ] Всё созданное **видно на публичном сайте** (через API задачи 03).

## Доступ / UX
- [ ] Экраны доступны только аутентифицированному (иначе редирект на login).
- [ ] Ошибки сервера показываются toast'ом, не роняют UI.

## Done
Админка функционально полная; серверный API не менялся; бэкап подключается отдельно (задача 11).
</content>
