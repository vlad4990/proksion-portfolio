# Verify — 12 deploy-hardening

## Запуск и здоровье
- [ ] Прод-подобный `make up` (с `SITE_ADDRESS=домен`) поднимает стек; Caddy выпускает HTTPS
      (Let's Encrypt) — сайт открывается по `https://`.
- [ ] `docker compose ps`: ровно `caddy`+`back`+`minio`, все `healthy`; контейнеров-сборщиков нет.
- [ ] Порядок запуска соблюдён (`back`/`caddy` ждут `minio` healthy); `restart: unless-stopped`.

## Безопасность
- [ ] Наружу опубликован только `caddy`. MinIO (API 9000 и консоль 9001) недоступны снаружи.
- [ ] Бэкенд ходит в MinIO отдельным app-ключом (не root). Public-read только на `images/*`.
- [ ] Секреты (`JWT_SECRET`,`ADMIN_PASSWORD_HASH`,`MINIO_*`,`S3_*`, токен облака) не в образе и
      не в репозитории; `.env` в `.gitignore`, `.env.example` без реальных значений.
- [ ] `/media/*` и хешированные ассеты отдаются с immutable-кэшем; сертификаты переживают пересоздание.

## Документация / синк
- [ ] Есть прод-деплой-гайд (`docs/deploy.md`): переменные, запуск, настройка `rclone.conf`, restore.
- [ ] Корневой `CLAUDE.md` обновлён (бэкенд реальный; `front`→`caddy`; сервисы/volumes/переменные);
      `front/CLAUDE.md` актуален; память проекта обновлена.

## Сквозной e2e-смоук
- [ ] Логин в админку → создать категорию/подкат/работу → загрузить картинки → задать описание/cover.
- [ ] Публичный сайт показывает контент (листинг + модалка с каруселью, шарящийся URL).
- [ ] После изменений (по дебаунсу) бэкап ушёл в облако (БД+картинки, порядок картинки→БД).
- [ ] Симуляция свежего деплоя (чистые volume) → restore из облака поднимает контент обратно.

## Done
Стек прод-готов, безопасен, задокументирован; сквозной сценарий зелёный.
</content>
