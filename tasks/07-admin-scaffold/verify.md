# Verify — 07 admin-scaffold

## Тесты (TDD)
- [ ] `cd admin && npm run test` (Vitest) — зелёный. Покрыты: API-клиент (заголовки, 401/ошибки),
      auth-стор (login/logout/восстановление сессии), `RequireAuth` guard, валидация формы логина.

## Сборка
- [ ] `cd admin && npm run build` — зелёный (tsc strict + vite build), `base:'/admin/'` соблюдён
      (ассеты префиксуются `/admin/`).

## Стек
- [ ] Tailwind применяется; shadcn/ui-компоненты (`button`,`input`,`form`,`dialog`,`table`,toast)
      установлены в репо и рендерятся.
- [ ] Зависимости админки — в `admin/package.json`, не протекли в `/front` (бандл `/front` не вырос).

## Флоу логина (против запущенного `back`)
- [ ] `/admin/login` — форма; верный пароль → редирект в dashboard, сессия активна (`/me` 200).
- [ ] Неверный пароль → ошибка, без редиректа.
- [ ] Прямой заход на защищённый роут без сессии → редирект на `/admin/login`.
- [ ] Logout сбрасывает сессию.
- [ ] Мутирующие запросы клиента несут CSRF-заголовок (совместимо с задачей 05).

## Done
Каркас админки готов; CRUD-экранов контента ещё нет (задача 08).
</content>
