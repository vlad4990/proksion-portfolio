# Verify — 11 backup-restore

## Тесты (TDD)
- [ ] `cd back && bun test` — зелёный. Покрыты: debounce (сброс таймера, single-flight,
      коалесинг), порядок шагов push, выбор версий для retention, ветвление restore (пусто/не пусто).
- [ ] Внешние команды (`rclone`, `VACUUM`) идут через инъектируемый раннер (в тестах — мок).

## Push (интеграция; cloud-remote может быть тестовым/local)
- [ ] После мутации в админке и истечения дебаунса (`BACKUP_DEBOUNCE_MINUTES`) в облаке
      появляются: `db/db.sqlite`, версия в `history/`, и зеркало картинок.
- [ ] Порядок соблюдён: **картинки залиты раньше БД** (БД не ссылается на отсутствующие в облаке картинки).
- [ ] Множественные изменения за окно дебаунса коалесятся в один прогон; одновременно не запускаются два.
- [ ] Retention: старые версии БД сверх `BACKUP_HISTORY_KEEP` удаляются.

## Restore (свежее окружение)
- [ ] На пустом окружении (чистые volume) старт тянет из облака сначала картинки в MinIO, затем БД.
- [ ] На непустом окружении restore НЕ перетирает данные (идемпотентный пропуск).
- [ ] Ждёт готовности `minio` перед синком картинок.

## Устойчивость
- [ ] При недоступном облаке приложение стартует и работает; ошибка бэкапа залогирована, не фатальна.
- [ ] Токен облака не попал в репозиторий/образ (только в `rclone.conf` через секрет/volume).

## Done
Автобэкап по изменению и restore-on-boot работают; полный прод-хардненинг — задача 12.
</content>
