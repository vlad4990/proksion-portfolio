# Verify — 16 admin-ui-listing-content

## Команды

```bash
cd admin && npx vitest run && npm run build
make dev-back && make dev-admin        # живой стек: бэкенд 3001, админка 5006
```

## Чек-лист (живой стек, после логина)

### Категория
- [ ] Форма категории показывает и сохраняет kicker / meta_role / period /
      description_long / display_variant; после сохранения `GET /api/categories/:cat`
      отдаёт новые значения; очистка поля пишет null.
- [ ] Невалидный variant невозможно отправить (select ограничен тремя значениями).

### Теги
- [ ] Создание тега с русским title → слаг-транслит виден в таблице.
- [ ] Переименование не меняет слаг; удаление — с подтверждением; после удаления
      тег исчез из `/api/tags` и из чипов в редакторе работы.
- [ ] Перестановка тегов сохраняется (порядок в `/api/tags` после F5).
- [ ] Дубль слага → видимая ошибка, состояние таблицы консистентно.

### Теги работы
- [ ] В редакторе работы виден текущий набор тегов; переключение чипов +
      сохранение → `WorkDetailById.tag_ids` обновился; снятие всех тегов работает.

### Витрина
- [ ] У категории без витрины — пустое состояние с подсказкой про fallback.
- [ ] Добавление 3 работ, перестановка, сохранение → публичный `/api/featured`
      отдаёт их в заданном порядке; первая помечена HERO в UI.
- [ ] Удаление работы из витрины и «Очистить витрину» работают (после очистки
      `/api/featured` снова отдаёт fallback).
- [ ] Работы без картинок в списке кандидатов не появляются.

### WorksPage (полировка)
- [ ] После F5 у работ с картинками виден слаг и активна кнопка «Управление»
      (без участия сессионного реестра); `works-view` тест обновлён.

### Регресс
- [ ] Существующие CRUD-потоки (категория/подкатегория/работа/картинки, cover,
      reorder) не сломаны — прогнать по разу; форма подкатегории не получила
      лишних полей категории.
- [ ] 401 (протухшая сессия) по-прежнему уводит на логин; ошибки мутаций видимы.
- [ ] Vitest: новые тесты логики (маппинг форм, наборы tag_ids, порядок
      work_ids) присутствуют и зелёные.

## Done

Все пункты + `docs/architecture.md` §8 упоминает новые экраны/блоки админки.
