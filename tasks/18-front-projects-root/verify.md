# Verify — 18 front-projects-root

## Команды

```bash
cd front && npm run build
make dev-back && cd front && npm run dev -- --port 5005   # живой API (сид или прод-дамп)
```

Данные для проверки: в БД должны быть теги/витрины/меты (сид задачи 13 или прод-дамп
с заполненной админкой). Для fallback-веток — категория без витрины и без тегов.

## Визуальная сверка (Chrome MCP на :5005 против фреймов Pencil)

- [ ] Desktop ≥1400: hero (оверлайн/титул+квадрат/подзаголовок/3 стата),
      ряд чипов + «NN РАЗДЕЛОВ ↓», секции всех трёх вариантов, футер — соответствие
      фрейму `tVnqG` (типографика, отступы, hairline-границы).
- [ ] Тиры 1100–1399 и 768–1099: композиция пересобирается токенами, ничего не
      ломается (горизонтального скролла нет).
- [ ] Mobile <768: соответствие `N8NrSi` (статы в ряд, горизонтальные чипы без
      переноса, витрины hero+пары, футер); текущий MobileTabBar на месте.

## Поведение

- [ ] Чипы: «ВСЕ» активен по умолчанию; клик по тегу → URL `/projects?tag=<slug>`,
      состояние переживает F5 и работает по прямой ссылке; cmd-клик открывает
      вкладку.
- [ ] Тег-режим: в секциях masonry работ с тегом; секции без совпадений скрыты;
      тег без работ вовсе → состояние «пусто»; возврат на «ВСЕ» — витрины без
      повторного запроса (кэш).
- [ ] Счётчики честные: сумма в чипе «ВСЕ» = сумме тегов? — нет, «ВСЕ» = общее число
      видимых работ; числа чипов совпадают с `/tags`.work_count; статы hero — с
      `/categories`.
- [ ] Секции: hero-тайл с пилюлей-подписью (title работы); все тайлы витрин —
      ссылки на канонические слаговые URL; модалка открывается и закрывается без
      сброса скролла листинга.
- [ ] «ВСЕ РАБОТЫ ↗» ведёт на `/projects/<cat>` (после задачи 19 — страница
      категории).
- [ ] «NN РАЗДЕЛОВ ↓» плавно скроллит к первой секции.
- [ ] Fallback'и: категория без витрины показывает первые работы; пустой `/tags`
      скрывает ряд чипов; NULL-меты (kicker/role/period) не рендерят пустых строк.
- [ ] Skeleton при холодной загрузке; ошибка сети → внятное состояние, не белый
      экран.
- [ ] LCP: первые витринные картинки eager/high, остальные lazy (проверить в
      Network/Elements).

## Регресс

- [ ] `/projects/:cat/:sub/:work` (модалка по прямой ссылке) работает поверх
      корневой… **нет** — теперь поверх страницы категории (задача 19); с корневой
      открытие модалки уводит на канонический URL и назад по закрытию (история).
- [ ] Занавес-герой на `/` и переходы TopNav/MobileTabBar не сломаны.
- [ ] `npm run build` зелёный; мёртвый код старого экрана удалён (grep по
      `projects-sidebar`, `groupTo` — пусто).

## Done

Все пункты + `front/CLAUDE.md` описывает новую корневую страницу.
