# Verify — 06 admin-api

## Тесты (TDD)
- [ ] `cd back && bun test` — зелёный. Покрыты: CRUD всех сущностей, загрузка/удаление картинок
      (интеграция с `minio`), reorder, авторизация, хук мутаций.

## CRUD
- [ ] Создание категории/подкатегории/работы генерирует уникальный слаг из title; правка/удаление
      работают; удаление родителя каскадит в БД.
- [ ] Слаг стабилен после создания (смена title не меняет слаг автоматически).

## Картинки
- [ ] `POST /api/admin/works/:id/images` (multipart) создаёт строку `image` + варианты в MinIO;
      первая картинка работы становится cover.
- [ ] `DELETE /api/admin/images/:id` удаляет строку И объекты S3; если это был cover — он
      переназначен/обнулён.
- [ ] `PATCH` обновляет alt/порядок.

## Reorder / доступ
- [ ] `reorder` персистит `sort_order` (публичный листинг 03 отражает новый порядок).
- [ ] Любой admin-эндпоинт без сессии → 401; без CSRF-условия → 403.

## Целостность
- [ ] Сбой заливки в S3 не оставляет висячих строк `image` (и наоборот) — проверено тестом/ручно.
- [ ] Успешная мутация триггерит `onMutation()` (хук вызван).

## TS
- [ ] `tsc --noEmit` strict — чисто, без `any`.

## Done
Серверный admin-API полный; UI (07–08) и rclone-бэкап (11) — отдельно.
</content>
