# Verify — 17 front-ds-shared

## Команды

```bash
cd front && npm run build          # tsc strict + vite build — зелёный
make dev-back                      # бэкенд на 3001 (данные — сид или прод-дамп)
cd front && npm run dev -- --port 5005
```

## Чек-лист

### Токены
- [ ] Новые токены добавлены в `:root` `tokens.css`, значения совпадают со спекой §3;
      существующие токены не изменены (git diff по файлу — только добавления).

### API-слой (проверка через dev-консоль/Network)
- [ ] `useTags`/`useFeatured`/`useCategory`/`useInfiniteWorks` возвращают данные
      живого бэкенда; повторный маунт того же ключа — 0 сетевых запросов (кэш).
- [ ] `useInfiniteWorks`: `loadMore` дозапрашивает с корректным offset; после
      конца списка запросы прекращаются (`hasMore === false`).

### Модалка (регресс + новое)
- [ ] Старая числовая ссылка `/projects/:cat/:sub/123` открывает модалку и
      `replace`-редиректит на слаговый URL (история не засоряется).
- [ ] Слаговый URL открывает модалку напрямую (deep-link, обновление страницы).
- [ ] `?img=` пиннит слайд; Esc/крестик/фон закрывают; счётчик N/M работает.
- [ ] Существующий листинг `/projects` (старый дизайн, тайлы с id) по-прежнему
      кликабелен и открывает модалку.

### Атомы и футер (визуал — временная страница/Storybook не нужны: смонтировать
на любом экране локально или проверить в 18–19)
- [ ] `FilterChip` active = заливка `--accent`; inactive = рамка `--hairline`;
      размеры/паддинги по дизайну (сверка с фреймом `tVnqG` в Pencil).
- [ ] `CountBadge`: «1 РАБОТА» / «2 РАБОТЫ» / «68 РАБОТ».
- [ ] `formatUpdated('2026-07-15 10:00:00')` → «ИЮЛЬ 2026».
- [ ] `ProjectsFooter` (оба дерева): вёрстка соответствует фреймам, ссылки
      email/TG/соцсети — из `lib/contacts.ts`, те же значения, что на
      `/contacts`.

## Done

`npm run build` зелёный; все пункты пройдены; `front/CLAUDE.md` отражает новый
api-слой, `components/shared/`, слаговую модалку.
