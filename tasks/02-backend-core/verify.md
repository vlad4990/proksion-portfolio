# Verify — 02 backend-core

## Тесты (TDD)
- [ ] `cd back && bun test` — все зелёные. Тесты есть для: `slug`, `migrate`, каждого
      репозитория, схемы/PRAGMA.
- [ ] Тесты не оставляют файлов в `/data` (используют `:memory:`/temp).

## Миграции
- [ ] Прогон с нуля создаёт все 4 таблицы (`category`,`subcategory`,`work`,`image`) + индексы.
- [ ] Повторный прогон идемпотентен (ничего не меняет, не падает).

## Схема и инварианты (§3)
- [ ] `PRAGMA journal_mode` → `wal`; `PRAGMA foreign_keys` → `1`.
- [ ] Вставка дубля слага в своей области нарушает UNIQUE (тест ловит ошибку).
- [ ] Удаление `category` каскадно удаляет её `subcategory`/`work`/`image`.
- [ ] Удаление `image`, на который ссылается `work.cover_image_id`, ставит его в `NULL` (не падает).

## Слаги
- [ ] `slugify('Брендинг')` → `brending`; пробелы/регистр/мусор нормализуются.
- [ ] Коллизии получают суффикс `-2`, `-3`, ...
- [ ] Пустой/непереводимый title → детерминированный fallback (не пустая строка).

## TS
- [ ] `bun run tsc --noEmit` (или эквивалент) — strict, без `any`, без ошибок.

## Done
Слой данных полностью протестирован; HTTP-эндпоинтов контента ещё нет (это задача 03).
</content>
