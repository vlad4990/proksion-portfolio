# Verify — 03 public-api

## Тесты (TDD)
- [ ] `cd back && bun test` — зелёный. Покрыты: `media-url`, сериализаторы, каждый эндпоинт
      (200+форма, 404, порядок, пагинация).

## Контракты ответов
- [ ] `GET /api/categories` → список с подкатегориями/счётчиками (форма из §7).
- [ ] `GET /api/categories/:cat/:sub` → тайлы строго формы `{ id, src, w, h }`; `src` — URL
      `thumb` cover; `w/h` — числа.
- [ ] `GET /api/works/:cat/:sub/:work` → описание + `images[]` с вариантами `thumb`/`full`
      в `avif`/`webp`/`jpg`, плюс `w/h`, `alt`, `sort_order` (и `lqip`, если задан).
- [ ] URL медиа имеют вид `/media/{key_base}/{variant}.{ext}`.

## Поведение
- [ ] Несуществующие `:cat`/`:sub`/`:work` → 404.
- [ ] Листинг отсортирован по `sort_order`.
- [ ] `/works` пагинируется по зафиксированному контракту (проверены границы: первая/последняя
      страница, пустой результат).

## Seed
- [ ] `bun run seed` наполняет БД примерами (категории→подкат→работы→картинки), идемпотентно.
- [ ] После seed `curl` эндпоинтов возвращает непустые корректные данные.

## TS
- [ ] `tsc --noEmit` strict — без ошибок, без `any`.

## Done
Публичный API отдаёт стабильные контракты; мутаций/аплоада нет (это задачи 04–06).
</content>
