# Verify — 15 admin-api-tags-featured

## Команды

```bash
cd back && bun test && bun run typecheck
```

## Чек-лист (покрыто тестами — проверить наличие и прохождение)

### Категории
- [ ] PATCH принимает и сохраняет kicker/meta_role/period/description_long
      (в т.ч. сброс в null) и display_variant.
- [ ] `display_variant: 'x'` → 400 `bad_request`, категория не изменена.

### Теги
- [ ] POST: 201, слаг сгенерирован из русского title транслитом; явный slug
      уважается; дубль слага → 400.
- [ ] PATCH: смена title слаг НЕ меняет; смена слага на занятый → 400; 404 по
      несуществующему id.
- [ ] DELETE: `{ok:true}`, строки `work_tag` удалены каскадом.
- [ ] `PATCH /admin/tags/reorder {ids}` переставляет `sort_order = index`
      (роут не перехватывается `/:id`).

### Теги работы
- [ ] PATCH works с `tag_ids` заменяет набор целиком; `[]` снимает все.
- [ ] Несуществующий tag id → 400, набор тегов работы не изменён.
- [ ] `tag_ids` работает одновременно с другими полями патча.
- [ ] После PATCH публичный `WorkDetailById.tag_ids` отражает новый набор.

### Витрина
- [ ] PATCH `/admin/categories/:id/featured {work_ids:[w3,w1]}` → публичный
      `/featured` отдаёт эту категорию с работами в порядке [w3, w1].
- [ ] Работа другой категории в списке → 400, витрина не изменена.
- [ ] Пустой список очищает витрину (у категории включается fallback в
      `/featured` — первые видимые работы).
- [ ] 404 по несуществующей категории.

### Сквозное
- [ ] Все новые мутации: 401 без cookie, 403 без `X-Requested-With`,
      `mutationCount()` растёт на успехе и НЕ растёт на 4xx.

## Done

Все пункты + `docs/architecture.md` §7 упоминает новые admin-роуты.
