# Verify — 10 front-modal

## Сборка
- [ ] `cd front && npm run build` — зелёный (tsc strict). Новых зависимостей в `/front` нет.

## Открытие/закрытие (визуально)
- [ ] Клик по тайлу открывает полноэкранную модалку работы; URL меняется на
      `/projects/:cat/:sub/:work`.
- [ ] `Esc` / клик по фону / кнопка закрытия → модалка закрывается, URL возвращается на
      `/projects/:cat/:sub`. Скролл фона блокировался на время показа.

## Карусель и контент
- [ ] Показываются описание и все картинки работы; `<picture>` отдаёт avif/webp/jpg (fallback
      работает), на время загрузки виден LQIP-фон.
- [ ] Навигация по слайдам работает; активный слайд синхронизирован с `?img=<imageId>`.

## Deep-link (шаринг)
- [ ] Прямой заход на `/projects/:cat/:sub/:work` открывает модалку поверх листинга.
- [ ] Ссылка с `?img=<imageId>` открывает нужный слайд.
- [ ] Несуществующая работа → корректный UX (редирект на листинг или 404), не белый экран.

## Оба дерева / доступность
- [ ] Работает и на десктопе, и на мобайле (эмуляция <768px).
- [ ] Фокус-трэп в модалке; кнопка закрытия с aria-label; занавес-герой/листинг не сломаны.

## Done
Модалка с каруселью и шарящимися URL работает в обоих деревьях.
</content>
