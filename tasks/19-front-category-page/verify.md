# Verify — 19 front-category-page

## Команды

```bash
cd front && npm run build
make dev-back && cd front && npm run dev -- --port 5005
```

Для инфинити-скролла нужна категория с >24 видимых работ — если в дампе такой нет,
временно уменьшить limit в вызове хука/сиде или добавить работ сидом (не коммитить).

## Визуальная сверка (Chrome MCP на :5005 против фреймов Pencil)

- [ ] Desktop ≥1400 соответствует `h16xA`: крошки+«ОБНОВЛЕНО», голова (оверлайн,
      титул+квадрат+бейдж, описание, меты, «НАПИСАТЬ ПО ПРОЕКТУ ↗»), чипы-табы +
      счётчик, masonry 4 колонки, футер.
- [ ] Тиры 1100–1399 (3 колонки) и 768–1099 (2 колонки) собираются токенами.
- [ ] Mobile <768 соответствует `fQvBp`: 2 колонки, горизонтальные чипы, счётчик.

## URL / табы

- [ ] `/projects/kupikod` — таб «ВСЕ» активен; клик по «СОЦСЕТИ» → URL
      `/projects/kupikod/social-network-posts`, чип активен, листинг —
      только подкатегория; «ВСЕ» возвращает на `/projects/kupikod`.
- [ ] Прямая ссылка / F5 на обоих вариантах URL восстанавливают состояние.
- [ ] Cmd-клик по чипу/тайлу открывает новую вкладку.
- [ ] Несуществующие `/projects/xxx` и `/projects/kupikod/xxx` → редирект на
      `/projects` (не белый экран).
- [ ] `/projects/uiux-keysy/boytsovskiy-klub` открывает **подкатегорию** (коллизия
      слагов не мешает: работа доступна как
      `/projects/uiux-keysy/boytsovskiy-klub/boytsovskiy-klub`).

## Инфинити-скролл и счётчик

- [ ] «ПОКАЗАНО 24 ИЗ M» после первой порции; скролл вниз догружает следующую
      (Network: `offset=24`), счётчик обновляется; после конца — запросы
      прекращаются.
- [ ] Быстрый скролл не порождает дублей запросов/тайлов (проверить Network и
      уникальность key).
- [ ] Смена таба сбрасывает грид на данные таба (без мигания старыми тайлами);
      возврат на прежний таб — мгновенно из кэша, догруженные страницы на месте.
- [ ] Догрузка не вызывает layout shift выше вьюпорта (aspect-ratio).

## Модалка (регресс)

- [ ] Клик по тайлу → модалка, URL `/projects/:cat/:sub/:slug`; Esc/крестик →
      возврат на листинг, скролл и догруженные тайлы сохранены.
- [ ] Deep-link на слаговый URL модалки: рендерится страница категории + модалка;
      закрытие ведёт на `/projects/:cat/:sub`.
- [ ] Легаси-числовой URL редиректится на слаговый (задача 17) и открывается.
- [ ] Карусель `?img=`, стрелки/свайп — как раньше.

## Регресс

- [ ] Переходы root ↔ category ↔ modal в обе стороны без потери состояния
      корневой (кэши).
- [ ] `document.title`: «KUPIKOD — PROKSION» на странице категории, имя работы —
      в модалке, возврат к «Проекты — PROKSION» на корневой.
- [ ] `npm run build` зелёный.

## Done

Все пункты + `front/CLAUDE.md` и `docs/architecture.md` §4 актуализированы.
